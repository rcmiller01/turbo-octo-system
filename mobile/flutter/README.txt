# Flutter Mobile UI (Placeholder)

This directory contains the initial Flutter scaffold.
To run:

```bash
cd mobile/flutter
flutter pub get
flutter run
```

You can expand this later to interact with on-device CoreML/TFLite models via platform channels or native integration.
